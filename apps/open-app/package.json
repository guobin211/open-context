{
  "name": "open-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm dev:all",
    "dev:app": "tauri dev --no-watch",
    "dev:web": "cd ./../../ && pnpm --filter open-web dev",
    "build:web": "cd ./../../ && pnpm --filter open-web build",
    "build:app": "tauri build",
    "build:all": "pnpm build:server && pnpm build:app",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6"
  }
}
